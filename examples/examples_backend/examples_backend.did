type Response = variant {
    network: text;
    state_version: nat64;
    proposer_round_timestamp: text;
    epoch: nat64;
    round: nat64;
}

service : {
    "example_transaction_construction": () -> (text) query;
}
